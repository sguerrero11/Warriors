<!DOCTYPE suite SYSTEM "https://testng.org/testng-1.0.dtd" >
<suite name="all-suites" parallel="classes" thread-count="3">
    <suite-files>
        <suite-file path="regression.xml" />
        <suite-file path="smoke.xml" />
    </suite-files>
</suite>

    <!-- if there is more than one class on each suite file, you can use the classes value -->
